# texdict - standalone CMake file for this example.
#
# Added automatically by the examples/ superbuild.

rw_add_example(texdict
  SOURCES src/main.c src/texdict.c src/win/events.c
  ASSET_DIRS models)
