# texadrss - standalone CMake file for this example.
#
# Added automatically by the examples/ superbuild.

rw_add_example(texadrss
  SOURCES src/main.c src/clump.c src/texadrss.c src/win/events.c
  ASSET_DIRS models)
