# memhints - standalone CMake file for this example.
#
# Added automatically by the examples/ superbuild.

rw_add_example(memhints
  SOURCES src/main.c src/memheap.c src/win/events.c
  ASSET_DIRS models)
