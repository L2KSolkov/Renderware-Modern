# lights2 - standalone CMake file for this example.
#
# Added automatically by the examples/ superbuild.

rw_add_example(lights2
  SOURCES src/main.c src/lights.c src/win/events.c
  ASSET_DIRS models)
