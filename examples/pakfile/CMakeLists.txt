# pakfile - standalone CMake file for this example.
#
# Added automatically by the examples/ superbuild.

rw_add_example(pakfile
  SOURCES src/main.c src/pakfile.c src/win/events.c
  ASSET_DIRS misc)
