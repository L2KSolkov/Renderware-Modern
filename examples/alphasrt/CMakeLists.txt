# alphasrt - standalone CMake file for this example.
#
# Added automatically by the examples/ superbuild.

rw_add_example(alphasrt
  SOURCES src/main.c src/alpha.c src/win/events.c
  ASSET_DIRS models)
